class Solution {
    public int missingNumber(int[] arr) {
        Arrays.sort(arr);
        int num =1;
        for(int i=0; i<arr.length; i++ ){
            if(arr[i]==num && arr[i]>=1){
                num++;
            }
            
        }
        return num;
        
    }
}
